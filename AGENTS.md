# SixDoku — Agent Context

> **Source of truth for specs: Obsidian Vault** — this repo keeps a `.md` reference + synced copy in `docs/` for future agents. Update both as the project evolves.

## 1. Vault Locations (canonical)
- **Bootstrap prompt (opencode):** `/Users/davechow/Documents/Local/Master Vault/06-Templates/OpenCodeBootstrapPrompt.md.md`
- **Project Home:** `/Users/davechow/Documents/Local/Master Vault/02-Projects/SixDoku/SixDoku - Project Home/`
- **README:** `/Users/davechow/Documents/Local/Master Vault/02-Projects/SixDoku/SixDoku - Project Home/README.md.md`
- **Specs root:** `/Users/davechow/Documents/Local/Master Vault/02-Projects/SixDoku/SixDoku - Project Home/Specs/`

### Specs by domain (18 files)
- **Product:** `Specs/Product/PDD_SixDoku.md.md`, `UIInteractionRules.md.md`, `ThemeSystem.md.md`, `DailyPuzzleSystem.md.md`, `iOSRoadmap.md.md`
- **Architecture:** `Specs/Architecture/SwiftUIArchitecture.md.md`, `CodingConventions.md.md`, `ViewModelContracts.md.md`, `BuildReleasePipeline.md.md`
- **Engine:** `Specs/Engine/SolverSpec.md.md`, `PuzzleGeneratorSpec.md.md`, `HintSystemSpec.md.md`, `TestingStrategy.md.md`
- **Cloud:** `Specs/Cloud/CloudKitSchema.md.md`, `ErrorHandlingSpec.md.md`, `DeveloperOnboarding.md.md`
- **API:** `Specs/API/APIReference.md.md`

## 2. Repo Locations (mirrored for agents)
- **This file:** `AGENTS.md` (agent bootstrap)
- **Mirrored docs:** `docs/Specs/` — full copy of vault Specs (generated by `scripts/sync-specs.sh`, keep in sync)
- **Repo root:** `/Users/davechow/Documents/Projects/SixDoku`
- **Plan:** `/Users/davechow/.claude/plans/effervescent-painting-arya-agent-a1fad010224fa4aa8.md` (xcodegen single project, Dev container `iCloud.com.sixdoku.dev`, MVP)

## 3. Agent Bootstrap
When starting a new session:
1. Read `AGENTS.md` (this file)
2. Read vault bootstrap + all `Specs/**/*.md` (vault paths above) — or fallback to `docs/Specs/` if vault not mounted
3. Follow `PDD > Architecture > API` precedence per bootstrap
4. Verify with `swift test` and `xcodebuild -scheme SixDoku` builds

## 4. Update Workflow
Specs live in **Obsidian first**, repo second:
- Edit in Obsidian Vault (`Specs/...`), then run:
  ```bash
  ./scripts/sync-specs.sh   # copies vault → docs/Specs/
  git add docs/Specs AGENTS.md && git commit -m "sync specs"
  ```
- Or edit in repo `docs/Specs/` and back-sync to vault manually (avoid drift). Prefer vault as editor.

## 5. Current Decisions (MVP)
- Single Xcode project via `project.yml` + `xcodegen` (not naked SPM)
- Dev CloudKit container `iCloud.com.sixdoku.dev` (Development env), `CODE_SIGN_ENTITLEMENTS` in `project.yml:49`
- Targets: `SharedCore`/`SharedServices` (`platform: auto`, watchOS+iOS), `SixDoku` (iOS 17), `SixDokuWatch` (watchOS 10)
- **App Store name: `6Doku`** (2026-09-04 — "SixDoku" taken in App Store Connect; bundle IDs unchanged: `com.sixdoku.app` / `com.sixdoku.app.watch`; display-name rename pending decision)

## 6. Quick Links
- Vault Product: `ca://s?q=PDD_SixDoku` etc. (if your Obsidian uses that scheme)
- Engine: `SharedCore/Engine/{Solver,Generator,Validator,BoxMapping}.swift` implements `APIReference.md.md`
